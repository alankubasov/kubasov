let museo, helvetica

let col_1, col_2, col_3



function setup () {
  createCanvas (720, 720)
  col_1 = color (0, 0, 150)
  col_2 = color (230, 0, 42)
  col_3 = color (20, 100, 0)
  noStroke ()
}

function draw () {
  background(col_1)

    
  fill(150, 49, 100);
  circle(400, 200, 63, 63);

  
  fill(150, 49, 100);
  rect(500, 200, 63, 63);

  
  for (let i = 0; i < 120; i++) {
    let p = (frameCount + i) / 160
    let y_off = abs (sin (p * PI * 2) * 120)
    const c = lerpColor (col_1, col_3, i / 180)
    fill (c)
    square (50 + ((i / 200) * 140) , 180 - y_off, 15)  

    
  }
     for (let i = 0; i < 170; i++) {
    let p = (frameCount + i) / 10
    let y_off = abs (sin (p * PI * 2) * 12)
    const c = lerpColor (col_2, col_1, i / 180)
    fill (c)
    circle (50 + ((i / 50) * 100) , 200 - y_off, 5)  
           fill (c)
    circle (10 + ((i / 1) * 10) , 300 - y_off, 20)  
                  fill (c)
    circle (10 + ((i / 1) * 10) , 400 - y_off, 30)  
                     fill (c)
    circle (0 + ((i / 1) * 10) , 500 - y_off, 40) 
           fill (c)
    circle (10 + ((i / 1) * 10) , 600 - y_off, 50) 
       
       
       
  }
  

  }
  
